services:
  backend:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis

  redis:
    image: "redis:latest"
    ports:
      - "6379:6379"
    command: redis-server --requirepass 12345678abcd --appendonly yes
